{% extends 'layout.njk' %}

{% block content %}
    <h1>Meteor Information</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Diameter (meters)</th>
                <th>Hazardous</th>
                <th>Close Approach Date</th>
            </tr>
        </thead>
            <tbody>
                {% for meteor in meteorData %}
                    <tr>
                        <td>{{ meteor.name }}</td>
                        <td>{{ meteor.diameter }}</td>
                        <td>{{ meteor.is_potentially_hazardous_asteroid }}</td>
                        <td>{{ meteor.close_approach_date_full }}</td>
                    </tr>
                {% endfor %}
            </tbody>
    </table>

    {% if wereDangerousMeteors %}
        <p>There were potentially hazardous meteors.</p>
    {% else %}
         <p>No potentially hazardous meteors were observed.</p>
    {% endif %}
    {% if count %}
        <p>Total number of meteors: {{ count }}</p>
    {% endif %}

{% endblock %}